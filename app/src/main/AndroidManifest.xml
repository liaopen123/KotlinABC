<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.qidongyinqing.kotlinabc">
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
     android:appComponentFactory="xxx"
        android:theme="@style/AppTheme"
        tools:replace="android:appComponentFactory">
        <activity android:name=".retrofit.RetrofitActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".coroutines.CoroutinesActivity">

        </activity>
        <activity android:name=".by关键字.ByActivity"></activity>
        <activity android:name=".method.MethodMainActivity" />
        <activity android:name=".coroutines.Coroutines1Activity" />
        <activity android:name=".xiecheng.XieChengActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Main2Activity" />

        <provider
            android:name=".app.Ktx"
            android:authorities="com.xx.KtxInstaller"
            android:exported="false" />
    </application>

</manifest>